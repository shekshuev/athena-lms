export enum ProfileRecordType {
  String = "string",
  Number = "number",
  Boolean = "boolean",
  Date = "date",
  Json = "json",
}

export const ALL_PROFILE_RECORD_TYPES = Object.values(ProfileRecordType);
